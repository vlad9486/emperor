module hello;
using system;

immutable int n = 5;
immutable data msg = "hello, world"0x0a;

func main = (out int result) {

    int i = 0;
    while (i < n) {
        printf(msg);
        i := i + 1;
    };
    result := 0;
};
