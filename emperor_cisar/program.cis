module hello

using
    system;

const
    n = 5i;

func main
local
    out int result;
    in char[] cline;
    int i;
do
    i := 0i;
    while (i < n)
        printf("hello, world\n");
        i := i + 1i;
    end
    result := n - i;
end

end

/*module (using var const type (enum struct) func (local do (while if)))*/
