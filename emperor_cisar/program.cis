module hello

using
    system;

const
    n = 5i;

func main
var
    out int result;
    in char[] cline;
    int i;
do
    i := 0i;
    while (i < n)
        printf("hello, world\n");
        i := i + 1i;
    end
    result := n - i;
end

end

/*module, using, func, do, var, const, if, while, type, enum, struct, end*/
