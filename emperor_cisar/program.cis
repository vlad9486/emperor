module hello;
using system;

u32 n = 5;
c8[] msg = "hello, world\n";

func(out u64 result, in c8[] arg) main {

    u32 i = 0;
    while (i < n) {
        printf(msg);
        i = i + 1i;
    };
    result = 0i;
};
