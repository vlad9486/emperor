module hello;
using system;

global immutable int n = 5;
global immutable array msg = "hello, world"0x0a;

func array main = (out int result) {

    local int i = 0;
    local bool condition = (i < n);
    while ^= (condition) {
        printf(msg);
        i = i + 1;
    };
    result = 0;
};
