module hello

using
    system;

obj main

/*default*/

end

interface main

(int n) property (immutable) n;
(int result) msg (obj) start (char[] cmd_line);

end

implementation main

(int result) msg start (char[] cmd_line)
local (int i)
do
    i := 0i;
    while (i < self.n)
        io.printf ("hello, world\n");
        i := i + 1i;
    end
    result := self.n - i;
end

end
